action (GetSchedule) {
  description (Get Overwatch league schedule)
  type (Search)
  collect {
    input (time) {
      type (TimeToFilterBy)
      min (Optional) max (One)
    }
  }
  output (Schedule) {
    throws {
      error (OffSeason) {
        on-catch {
          halt {
            dialog {
              template ("#{macro('off-season')}")
            }
          }
        }
      }
    }
  }
}